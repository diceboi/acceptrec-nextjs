import { ReactNode, ComponentType } from 'react';
import type { ShareOptions } from '../hooks/useShare';
export declare type LoginButton = ShareOptions & {
    children?: ReactNode;
    asChild?: ComponentType | keyof JSX.IntrinsicElements;
    disabled?: boolean;
};
export default function ShareButton(props: LoginButton): JSX.Element;
